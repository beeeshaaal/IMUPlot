{
  "image": "mcr.microsoft.com/devcontainers/java:8",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack"
      ]
    }
  },
  "postCreateCommand": "curl -L https://services.gradle.org/distributions/gradle-3.3-bin.zip -o gradle.zip && unzip gradle.zip -d /usr/local && ln -s /usr/local/gradle-3.3/bin/gradle /usr/local/bin/gradle && gradle -v"
}
